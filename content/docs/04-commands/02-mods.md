# Mods commands

Everyone with the `Manage Messages`-permission has access to these commands.

:::caution Note

The default prefix `c!` will change soon to the new Discord `/`commands.

:::

- **/listflows:** -  Get a list of all the flows.
- **/listregex:** -  Get a list of regex filters.
- **/setcount:** -  Set the count.